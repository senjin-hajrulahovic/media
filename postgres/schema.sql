\c media;

create table media(
   ID INT PRIMARY KEY      NOT NULL,
   title           CHAR(50) NOT NULL
);

create table mediaz(
   ID INT PRIMARY KEY      NOT NULL,
   title           CHAR(50) NOT NULL
);